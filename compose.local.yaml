services:
  portfolio:
    image: nginx:alpine
    container_name: donngas-landing-page-local
    restart: unless-stopped
    ports:
      - "127.0.0.1:${LOCAL_PORT:-8234}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./public:/usr/share/nginx/html:ro
